To login/connect Remote Repo and git bash
   
    git config --global user.name "username"
    git config --global user.email "emailaddress"   

To create and checkout the code on new branch

    git checkout -b branch name

To push that origin to Remote Repository 
   
    git push origin branch name
    
To push the file to git hub account

    git add file name
    git commit -m "commit message"
    git push
    token
    git status
